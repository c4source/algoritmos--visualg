algoritmo "soma"
var
  n, s, cont: inteiro

inicio
   escreva("digite um número: ")
   leia(n)
   cont <- 1
   s <- 0
   enquanto cont <= n faca
    escreval (cont)
    cont <- cont + 1
    s <- s + cont
    fimenquanto
    escreval("soma é: ",s)


fimalgoritmo